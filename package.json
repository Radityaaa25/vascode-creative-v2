{
  "name": "vascode_v2",
  "private": true,
  "packageManager": "pnpm@9.4.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm -r --parallel dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1",
    "turbo": "latest"
  }
}
